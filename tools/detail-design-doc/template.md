# 詳細設計書：<対象名>

<!-- ガイド: 章の順序・見出し・表の列は変えない。該当なしの章は削除せず
「該当なし（確認済み: <どこを見たか>）」と書く。
確度は3段階で明示する: 直接の裏付けあり=確定（通常・無印）／
命名・パターン・呼出関係から導けるが裏付けなし=該当箇所に〔推定〕を付し断定形で書かない
（設計意図・rationale 等）／ 安全に判断できない=第11章へ隔離（本文に埋めない）。
コードに存在しない項目（画面項目・表・列・設定・メッセージ）を創作しない。 -->

| 項目 | 内容 |
|---|---|
| ドキュメントID | DD-XXX-0000 |
| システム名 | <対象システム名> |
| 対象画面 / 機能名 | |
| 対象クラス（起点） | <Form名>（本文にはクラス名を散らさない。ここと付録のみ） |
| 作成者 / 作成日 | |
| ステータス | Draft / Reviewed / Approved |

## 更新履歴

| 版 | 日付 | 変更者 | 変更内容 |
|---|---|---|---|
| 0.1 | | | 初版（実装からの書き起こし） |

## 1. 概要

### 1.1 機能の目的
<!-- この画面/機能が業務上何のためにあるか。1〜3文。 -->

### 1.2 スコープ
<!-- 対象・対象外。対象外は「→ 別設計書ID」で参照。 -->

### 1.3 前提条件・制約
<!-- 起動条件（親画面・権限・患者選択済み等）、動作環境上の制約。 -->

### 1.4 用語定義

| 用語 | 定義 |
|---|---|

## 2. 位置づけ・呼び出し関係

### 2.1 呼び出し元
<!-- この画面/機能をどこから開くか（メニュー・親画面・ショートカット）。
     Grep（new <Form>( / ShowDialog）で確認した結果を業務語で。
     複数の入口（FunctionKey 等）が「同一画面を特定タブ/パラメータ初期選択で開くだけ」の
     ランチャーである場合は、各入口を1行ずつ挙げ「どの状態で開くか（初期タブ等）」を
     引き渡し情報に書く。共有機構（タブ・確認ステータス・CRUD・設定）の詳細は本画面側に
     集約し、入口ごとに重複させない。 -->

| 呼び出し元 | 契機（操作） | 引き渡される情報（初期タブ・対象患者等） |
|---|---|---|

### 2.2 遷移先・連動する画面

<!-- 本画面から開く子画面・遷移先を2種に分けて書く。判別は Step 1 の判定（全開放点の確認）に従う。
     (A) 専属画面 = この画面/機能からのみ開かれ、単独では意味を持たない子ダイアログ・子画面。
         別設計書に出さず、本設計書内に章を設けて詳細化する（分割出力では専属画面ごとに
         章ファイル `0X_<和名>ダイアログ.md` を作る。Step 7 参照）。ここでは要点と本書内の章参照のみ。
     (B) 共有画面 = 本機能画面集合（対象画面＋取り込む専属子孫の閉集合）の外側からの開放点が
         1つでもある画面（患者検索・職員検索・定型文テンプレート等）。別設計書とし、ここでは
         存在・入出力の要点に加え「判定根拠（機能外のどの開放点で共有としたか）」と
         「別設計書の状態」を記す。状態は 既存/未作成/対象外 の3値。状態=未作成 のものは
         第11章 `未処理` に一元集約する（本表の状態列＝構造化された事実記録、第11章＝人間向け行動総账）。
     専属/共有の判別が曖昧（ある開放点が集合外か確信が持てない）なものは保守的に (B) 共有として扱う。 -->

**(A) 連動する専属画面（本設計書内で詳細化）**

| 専属画面 | 契機 | 受け渡す情報 | 本書内の章 |
|---|---|---|---|

**(B) 共有画面・その他の遷移先（別設計書）**

| 遷移先 / 連動先 | 契機 | 受け渡す情報 | 判定根拠（機能外のどの開放点で共有としたか） | 参照（別設計書ID等） | 別設計書の状態（既存/未作成/対象外） |
|---|---|---|---|---|---|

## 3. 画面設計

### 3.1 画面項目定義
<!-- Designer.cs の InitializeComponent() から。コントロール種別は WinForms 型名を英語で。
     グリッド（DataGridView）はここでは1行に留め、詳細は第4章に展開。 -->

| No | 項目名（表示） | 項目ID（コントロール名） | 種別 | 初期状態 | 必須 | 説明 |
|---|---|---|---|---|---|---|

### 3.2 画面イベント一覧
<!-- Designer.cs の結線から網羅。処理内容の詳細は第5章へ。 -->

| No | 項目ID | イベント | 動作概要 | 詳細 |
|---|---|---|---|---|
| 1 | | Click | | → §5.2 P-01 |

### 3.3 入力チェック（バリデーション）
<!-- CheckValidationControls / 入力チェックコードから。 -->

| 対象項目 | チェック内容 | NG時の動作（メッセージID） |
|---|---|---|

### 3.4 コンテキストメニュー（右クリックメニュー）
<!-- 該当する場合のみ。グリッド/コントロールに割り当てた ContextMenuStrip を
     Designer.cs（ContextMenuStrip 割当）と Opening ハンドラ（表示・活性条件）から。
     クリック時の処理詳細は第5章（P-xx）へ回し、ここでは条件と動作概要に留める。
     割当が無ければ「該当なし（確認済み: Designer に ContextMenuStrip 割当なし）」。 -->

| 割当先（コントロール） | メニュー項目 | 表示・活性条件 | クリック時の動作（概要） | 詳細 |
|---|---|---|---|---|

### 3.5 条件付き表示制御（施設・設定・患者状態による分岐）
<!-- 施設判定・設定・権限・患者状態（VIP/入院有無等）で、項目やタブの
     表示/非表示・活性/非活性・スタイル・文言・タブ名が切り替わるものを1か所に集約する。
     ★施設・環境分岐は「〜あり」で濁さず、条件を1つずつ具体列挙する（対象施設ぶん全て）。
     切替の判定キー・既定値は §8 に集約し、ここは分岐条件と切替結果のみ書いて §8 を相互参照する
     （既定値・定義位置をここに重複させない）。無ければ「該当なし（確認済み: …）」。 -->

| 対象（項目/タブ/コントロール） | 切替内容（表示・活性・スタイル・文言） | 分岐条件（キー/施設/権限/患者状態） | 参照 |
|---|---|---|---|

## 4. 検索・一覧（グリッド）設計
<!-- グリッドごとに 4.x を繰り返す。表示列・検索パラメータ・返却項目の3表を必ずそろえる。
     第6章の SELECT と条件/取得カラムが一致していること。グリッドが無い場合は
     「該当なし（確認済み: Designer.cs に DataGridView なし）」。 -->

### 4.1 <グリッド名>

**(1) 検索パラメータ（入力条件）**

| No | 条件項目 | 対応する画面項目ID | 型 | 必須 | 検索方式 | 備考 |
|---|---|---|---|---|---|---|

**(2) 表示列定義**

| No | 列名（ヘッダ） | 列ID | 型 | 幅 | 表示順 | 書式 | バインド元（返却項目） |
|---|---|---|---|---|---|---|---|

**(3) 返却項目（取得カラム）**

| No | 返却項目 | 取得元テーブル.カラム | 型 | 備考 |
|---|---|---|---|---|

**(4) 検索仕様の共通ルール**
<!-- 0件時の動作、件数上限、初期ソート、行選択/ダブルクリック時の動作。 -->

## 5. 処理設計

### 5.1 処理一覧

| No | 処理名 | 契機（画面操作/ライフサイクル） | 概要 |
|---|---|---|---|
| P-01 | | | |

### 5.2 処理詳細（処理ごとに記述）

#### P-01: <処理名>

| 項目 | 内容 |
|---|---|
| 契機 | |
| 入力 | |
| 出力 / 画面反映 | |
| 前提条件 | |
| 事後条件 | |

**処理の流れ**（日本語の番号手順。コード・擬似コードは書かない。
設定による分岐は「設定〈キー名〉が○のとき…」と条件として明記する。既定値・定義位置は §8 のみ）

1.
2.

### 5.3 複雑ロジックの記述（該当する処理のみ）
<!-- 複雑度に応じて選ぶ。単純な処理は 5.2 の手順だけで足りる。
     L1: 単純 → 手順のみ
     L2: 多分岐（3分岐以上） → 「条件 → 結果」表
     L3: 状態を持つ → 状態遷移表（または Mermaid 状態遷移図）
     L4: 算定・用量・数量・金額の計算 → 日本語の計算手順＋数式（コードではなく） -->

**条件 → 結果 表の例**

| 条件 | 結果 |
|---|---|
| 〜かつ〜の場合 | 〜する |

**状態遷移表の例**

| 現在状態 | 契機 | 遷移先 | 付随する動作 |
|---|---|---|---|

### 5.4 横断的処理（画面共通の振る舞い）
<!-- 個々のイベント/UC ではなく、画面全体・複数処理にまたがる「業務に関わる」共通の振る舞いを扱う。
     ★対象＝業務横断（この画面/機能に固有の業務的意味を持つもの）。下表はその観点チェックリスト。
     ★対象外（原則書かない）＝アプリ全体で一律に走る基盤機構: 監査/アクセスログ・DBトリガ（変更履歴）・
        基底の共通例外捕捉・一般的なライフサイクルフック・自動ログオフ 等。全画面共通で本画面固有の情報に
        ならないためノイズになる。本画面だけの差異・特別扱い（例: この画面だけ追加の監査項目を残す）が
        あるときのみ1行で記す。
     この表の役割＝画面内の全横断処理を一覧し漏れを防ぐ「見取り図（索引）」。詳細を全部ここに詰め込む場所ではない。
     詳細の居場所は横断が発生する処理フロー: §5.2 の P-xx（L2〜L4 なら §5.3）や §3.5・§6.2 等。
     横断はそこに自然に書いてよい／書くべき。ここは各行を「要約1〜2文＋『詳細箇所』でその P-xx/章を指す」に留める。
     ★フロー等に書いた横断は、必ずこの表にも1行載せる（載せ忘れ＝散在・漏れ）。逆に、
       画面全体に一律で効く等フローに単一の居場所が無い横断のみ、ここに直接記述してよい。
     他章に居場所がある項目（権限/VIP→§3.5・§8、Tx境界/排他→§6.2）は相互参照のみで重複させない。
     ★横断か否かの判定本質＝「1つの動作/状態変化が多数の箇所（複数コントロール・複数出口・複数画面）に
        扇出（fan-out）し、1つ漏らすと事故になる」もの。契機が単一・影響が局所の直線的な処理は横断ではない（→§5.2 の普通の P-xx）。
     ★下表は業務系システムで漏れやすい観点の定番リスト。多くの画面では大半が「該当なし」になるが、それが正常
        （逐条確認させることで漏れを防ぐのが目的）。無理に何かを当てはめない。
     ★「同時実行制御」行は悲観ロック（排他による編集可否）と楽観ロック（保存時の競合検知・他端末更新の最新化）の
        両面を扱う。「ダーティチェック＋未保存離脱ガード」行は変更検知と離脱時ガードの一体両面を扱う。
     ★「初期表示の状態決定」はモード/状態/ロック/権限の合成規則。逐コントロールの表示制御は §3.5、
        ここには「複数条件が競合したときどれが勝つか（優先規則）」を索引として記す（医療系で最も漏れやすい）。
     ★各行を必ず埋める。該当ある項のみ具体化し、無い項目は「該当なし（確認済み: <どこを見たか>）」。 -->

| 横断処理（業務観点） | 本画面での実現（有無・トリガ・対象範囲。複雑なら要約のみ） | 詳細箇所 |
|---|---|---|
| モードによる画面全体の振る舞い切替（新規/編集/コピー/参照 等） | | |
| 項目間の連動・再計算（入力変更が関連項目・合計・整合に波及） | | §5.3 |
| 状態の画面横断的な伝播・反映（中止/確定/ロック等のステータス） | | §5.3 |
| 同時実行制御（排他＝悲観ロック／競合＝楽観ロック・他端末更新の最新化） | | §6.2 |
| ダーティチェック＋未保存離脱ガード（変更検知／閉じる・切替・遷移時の確認・破棄・保存後遷移） | | §3.3 / §9.1 |
| 権限・閲覧制限（VIP）による業務操作の可否 | | §3.5 / §8 |
| 対象（患者等）切替時の一括再読込・整合 | | §5.1 |
| 入力チェックの横断ルール（検証時機＝失焦/保存時、複数エラーの集約提示、再計算後の再検証） | | §3.3 / §9.1 |
| 処理中の操作抑止・二重送信防止（非同期実行中の操作ロック、冪等性、処理中の対象切替） | | §5.2 |
| 失敗時の画面状態復旧（保存/通信失敗後: 編集内容の保持、ロック解放、ロールバック） | | §5.2 / §9.2 |
| 初期表示の状態決定（モード/状態/ロック/権限の合成・優先規則による各コントロールの初期可否） | | §3.5 |

## 6. ユースケース設計（データ入出力）
<!-- この章は後工程で AI が DDD アプリケーション層のインターフェースメソッドを生成する入力になる。
     したがって「表に何をするか」ではなく「業務として何を依頼するか（ユースケース）」を主語にして書く。
     SQL・CRUD 表は現行挙動の裏付けであり、契約はユースケース定義（6.2）が正。 -->

### 6.1 ユースケース一覧
<!-- DB 入出力を伴う操作を、UI 非依存の業務動詞で命名する（「検索ボタン押下」ではなく「検査結果を検索する」）。
     画面処理 P-xx との対応を必ず示す。1つの P-xx が複数 UC に分かれてもよい。
     UI のみの処理（Button 有効化・フォーカス移動等）は UC にしない。 -->

| UC-ID | ユースケース名（業務動詞） | 種別 | 契機（処理No） | 概要 |
|---|---|---|---|---|
| UC-01 | | 照会 / 登録 / 更新 / 削除 | P-xx | |

### 6.2 ユースケース定義（UCごとに記述）

#### UC-01: <ユースケース名>

| 項目 | 内容 |
|---|---|
| 種別 | 照会（Query）/ 更新系（Command） |
| 契機 | P-xx（→ §5.2） |
| 事前条件 | <!-- 業務上の前提。「患者が特定されていること」等 --> |
| 事後条件 | <!-- 成功時に業務として保証される状態 --> |
| トランザクション境界 | <!-- この UC 内で一括コミットする範囲。照会なら「なし」 --> |
| 同時実行・排他 | <!-- 楽観/悲観、なしなら「考慮なし（現行どおり）」 --> |

**入力（→ アプリケーション層の入力DTOに対応）**
<!-- グリッド検索なら §4.1(1) 検索パラメータと1対1で一致させる。 -->

| No | 項目 | 型 | 必須 | 制約・意味 |
|---|---|---|---|---|

**出力（→ アプリケーション層の出力DTOに対応）**
<!-- グリッド表示なら §4.1(3) 返却項目と1対1で一致させる。件数上限・打切りフラグ等の
     メタ情報も出力の一部として書く。 -->

| No | 項目 | 型 | 意味 |
|---|---|---|---|

**業務ルール（不変条件）**
<!-- この UC が常に守るべき業務条件。§5.3 の条件→結果表を参照してよい。 -->

**結果とエラーの対応**
<!-- 業務上の失敗（入力不正・0件・上限超過・排他競合）と技術例外（通信断）を区別する。
     業務上の失敗は「結果」として返し、メッセージID（§9）と対応づける。 -->

| 結果 | 分類 | 対応メッセージ | 備考 |
|---|---|---|---|
| 成功 | 正常 | - | |
| | 業務エラー（結果として返す） | M-xx | |
| | 技術例外 | - | |

### 6.3 UC → CRUD 対応表
<!-- UC 単位で、どのテーブルに何をするか。 -->

| UC-ID | テーブル | C/R/U/D | 主な条件 | 主な対象カラム |
|---|---|---|---|---|

### 6.4 SQL定義（現行挙動の参考）
<!-- 手書き SQL を用いるシステムでは原則記載。DB アクセス層（呼び出し先クラス）を Read して
     実際に発行される形を書く。バインド変数はそのまま。テーブル・カラムはスキーマ/エンティティ定義で実在確認。
     SELECT/INSERT/UPDATE の列は「…」「その他」等で省略せず列名を全列挙する（要約に潰さない）。
     例外: 連番の自明な繰り返し列（PatientFlag01〜21 等）は範囲表記でよいが、その旨を注記する
     （「PatientFlag01〜21 の21列すべて」等）。非連番の業務列は1つも省かない。
     注意: この SQL は「現行実装がこう動いている」という記録であり、移行先の実装がこの SQL・
     結合構造を踏襲する契約ではない。契約は 6.2 のユースケース定義。 -->

```sql
-- Q-01: <用途>（対応 UC: UC-xx）
```

| 項目 | 内容 |
|---|---|
| 対応 UC | UC-xx |
| 実行経路 | 画面操作 → サーバコマンド（業務語で） |
| 想定件数 / 性能上の注意 | |

### 6.5 テーブル・カラム定義（本機能が参照/更新する範囲のみ）

| テーブル | カラム | 型 | 本機能での用途 | 実在確認 |
|---|---|---|---|---|
| | | | | ○ / 未確認 |

## 7. 外部連携
<!-- 他システム・ファイル出力・印刷等。無ければ「該当なし（確認済み: …）」。 -->

| 連携先 | 方式 | 契機 | データ内容 |
|---|---|---|---|

## 8. 設定値

<!-- <何か>Settings.<設定名>.Value() の Grep 結果から。既定値は Appender で確認。
     設定が処理を分岐させる場合、その分岐は §5 の処理の流れ／条件→結果表に
     「設定〈キー名〉が○のとき…」の形で書き、UC の挙動を変える場合は §6.2 の
     入力項目または事前条件として明示する。本章は集中管理表であり、
     既定値・定義位置はここにのみ書く（各章で重複させない）。
     設定キーだけでなく、環境判定・施設判定（例: HospitalID 一致・グループ判定）・
     機能ライセンス・権限による分岐も、当画面の挙動を変えるものは本表に1行ずつ登録する。
     区分列でそれらを区別し、施設分岐は対象施設をすべて列挙する。 -->

| 設定キー / 判定 | 区分 | 既定値 | 本機能での影響 | 影響箇所（章参照） |
|---|---|---|---|---|
<!-- 区分: 設定 / 施設判定 / 環境判定 / 機能ライセンス / 権限 -->


## 9. メッセージ・エラー処理

### 9.1 メッセージ一覧
<!-- MessageGenerator.* から。戻り値（はい/いいえ等）による分岐も書く。 -->

| ID | 種別 | 文言 | 表示契機 | 応答による分岐 |
|---|---|---|---|---|

### 9.2 異常時の動作

| 事象 | 検知 | 画面の動作 | データへの影響 |
|---|---|---|---|

## 10. 医療安全上の留意点
<!-- 患者識別・用量・処方中止/変更の伝播・外部送信・診断/検査/フラグの表示記録・算定期間など、
     この画面の「仕様として」注意して扱う点・設計意図・制約。
     実装バグ・規約違反の指摘は書かない（→ spec-impl-reconcile）。 -->

## 11. 未確認事項・申し送り（要人間確認の一元収集）
<!-- この章は「AI が確定できなかった／処理しきれなかった／確信を持てなかった」事項を一元集約する
     唯一の収集点。人間はこの章を読めば、本設計書で人手の確認・判断が要る箇所を漏れなく把握できる。
     収集する4分類:
       未確認    = 安全に判断できなかった事実（テーブル/カラム/設定/文言の実在等）。従来どおり。
       理解不完全 = 追跡が途中（複雑ロジック/動的SQLを解ききれない、子画面の開放点を追い切れない等）。
       未処理    = スコープ外にしたが対応が要る依存（共有と判定し別設計書が未作成＝§2.2表(B)状態=未作成、
                   外部依存等）。§2.2 表(B) の該当行と交叉参照し、正文を重複させない。
       低信頼    = 〔推定〕を多用した／自動生成図（Mermaid 等）など置信度が低い区域。
     ★〔推定〕（裏付けは無いが合理的に導ける記述）は本文に置く。ここへ入れるのは真の gap のみ。
       「推断したもの全部」を倒し込まない（ダンプ化の防止）。確度分層 確定/推定/未確認 を維持する。
     ★影響列は必ず埋める（triage 用）。後工程の DDD 生成をブロックするか、医療安全に触れるかを明示。
       医療安全に触れる行は行頭に【医療安全】。
     ★本章が捉えるのは known gaps（自分が搞不定と分かっているもの）のみ。「搞定したつもりで誤り」
       （unknown unknowns）は捉えられない → spec-impl-reconcile の全量突合・人手の網羅確認で担保する。 -->

| No | 分類 | 内容 | 影響（後工程DDD生成/医療安全をブロックするか） | どこまで追ったか | 人間への依頼（何を確認/判断してほしいか） |
|---|---|---|---|---|---|

## 付録A. 解析対象ファイル一覧（網羅の証明）

| ファイル | 用途（Designer / イベント本体 / サーバコマンド / モデル / 設定 …） |
|---|---|

## 付録B. トレーサビリティ（必要な場合のみ・最小限）

| 設計書の記載箇所 | 実装位置（file:line） |
|---|---|
