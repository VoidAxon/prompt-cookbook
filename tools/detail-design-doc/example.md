# 詳細設計書：検査結果一覧照会画面

<!-- この example は架空の画面によるサンプル。実プロジェクトの仕様ではない。
     「各章をどの粒度・どの書き方で埋めるか」の見本として参照する。 -->

| 項目 | 内容 |
|---|---|
| ドキュメントID | DD-LAB-0021 |
| システム名 | <対象システム名>（架空サンプル） |
| 対象画面 / 機能名 | 検査 / 検査結果一覧照会 |
| 対象クラス（起点） | FrmLabResultList |
| 作成者 / 作成日 | 張 / 2026-07-12 |
| ステータス | Draft |

## 更新履歴

| 版 | 日付 | 変更者 | 変更内容 |
|---|---|---|---|
| 0.1 | 2026-07-12 | 張 | 初版（実装からの書き起こし） |

## 1. 概要

### 1.1 機能の目的
選択中の患者について、過去の検査結果を期間・検査種別で絞り込んで一覧表示し、行を選択して結果詳細画面を開くための照会画面。参照専用であり、本画面から検査データの登録・修正は行わない。

### 1.2 スコープ
- 対象：検索条件の入力、結果一覧の表示、詳細画面への遷移、異常値の強調表示
- 対象外：検査オーダの発行（→ DD-ORD-0005）、結果詳細の表示内容（→ DD-LAB-0022）

### 1.3 前提条件・制約
- カルテ画面で患者が選択済みであること（患者未選択では本画面を開けない）
- 参照専用。本画面からのDB更新は一切行わない
- 表示対象は確定済み結果のみ。未確定（速報）結果は設定（§8）により表示可否が切り替わる

### 1.4 用語定義

| 用語 | 定義 |
|---|---|
| 基準値外 | 検査項目ごとに定義された基準範囲（下限〜上限）を外れた結果値 |
| 速報 | 検査部門で最終確定前に送信される暫定結果 |

## 2. 位置づけ・呼び出し関係

### 2.1 呼び出し元

| 呼び出し元 | 契機（操作） | 引き渡される情報 |
|---|---|---|
| カルテ画面 | メニュー「検査」→「結果一覧照会」 | 患者ID、カルテ表示中の診療日 |
| 検査オーダ画面 | 「過去結果」Button の Click | 患者ID、選択中の検査種別コード |

### 2.2 遷移先・連動する画面

| 遷移先 / 連動先 | 契機 | 受け渡す情報 |
|---|---|---|
| 検査結果詳細画面 | 一覧行のダブルクリック / 「詳細」Button | 検査依頼番号、患者ID |

## 3. 画面設計

### 3.1 画面項目定義

| No | 項目名（表示） | 項目ID（コントロール名） | 種別 | 初期状態 | 必須 | 説明 |
|---|---|---|---|---|---|---|
| 1 | 患者ID | txtPatientId | TextBox | 読取専用・呼出元の患者IDを表示 | - | 本画面では変更不可 |
| 2 | 患者氏名 | lblPatientName | Label | 患者IDに対応する氏名を表示 | - | |
| 3 | 検査日From | dtpDateFrom | DateTimePicker | 診療日の3か月前 | ○ | |
| 4 | 検査日To | dtpDateTo | DateTimePicker | 診療日 | ○ | |
| 5 | 検査種別 | cmbTestCategory | ComboBox | 「全て」 | - | 検査種別マスタから取得。呼出元が種別を渡した場合はその種別を初期選択 |
| 6 | 基準値外のみ | chkAbnormalOnly | CheckBox | OFF | - | |
| 7 | 検索 | btnSearch | Button | 有効 | - | |
| 8 | 結果一覧 | dgvResults | DataGridView | 空 | - | 詳細は §4.1 |
| 9 | 詳細 | btnDetail | Button | 無効（行選択で有効化） | - | |
| 10 | 閉じる | btnClose | Button | 有効 | - | |

### 3.2 画面イベント一覧

| No | 項目ID | イベント | 動作概要 | 詳細 |
|---|---|---|---|---|
| 1 | （画面） | Load | 患者情報の表示、検査種別マスタの取得、検索条件の初期化 | → §5.2 P-01 |
| 2 | btnSearch | Click | 入力チェック後、条件に従い結果一覧を検索・表示 | → §5.2 P-02 |
| 3 | dgvResults | SelectionChanged | 行選択の有無で「詳細」Button の有効/無効を切替 | 概要のみで足りる |
| 4 | dgvResults | CellDoubleClick | 選択行の結果詳細画面を開く | → §5.2 P-03 |
| 5 | btnDetail | Click | 同上（P-03 と同一処理） | → §5.2 P-03 |
| 6 | btnClose | Click | 画面を閉じる | 概要のみで足りる |

### 3.3 入力チェック（バリデーション）

| 対象項目 | チェック内容 | NG時の動作（メッセージID） |
|---|---|---|
| dtpDateFrom / dtpDateTo | From ≦ To であること | M-01 を表示し検索を中止。フォーカスを dtpDateFrom へ |
| dtpDateFrom / dtpDateTo | 期間が2年以内であること | M-02 を表示し検索を中止 |

### 3.4 コンテキストメニュー（右クリックメニュー）

結果一覧グリッド（dgvResults）に contextResult（ContextMenuStrip）を割り当てる。他のコントロールには割当なし（確認済み: Designer に他の ContextMenuStrip 割当なし）。いずれの項目も画面内で完結し、DB更新・画面遷移は伴わない。

| 割当先（コントロール） | メニュー項目 | 表示・活性条件 | クリック時の動作（概要） | 詳細 |
|---|---|---|---|---|
| dgvResults | 結果値をコピー | セル上で右クリックしたときのみ表示（Opening で HitTest がセル以外なら e.Cancel）。行選択中は常に活性 | 選択行の結果値・単位をクリップボードへコピーする | 概要のみで足りる |
| dgvResults | 基準値をコピー | 基準値列（colReferenceRange）のセル上で右クリックしたときのみ表示 | 選択行の基準値（下限〜上限）をクリップボードへコピーする | 概要のみで足りる |

### 3.5 条件付き表示制御（施設・設定・患者状態による分岐）

| 対象（項目/タブ/コントロール） | 切替内容（表示・活性・スタイル・文言） | 分岐条件（キー/施設/権限/患者状態） | 参照 |
|---|---|---|---|
| 確定区分列（colStatus）・速報行のグレー表示 | 設定 OFF のとき速報を扱わないため確定区分列を非表示にする。ON のとき速報行を文字色グレーで表示し確定結果と区別する | 設定〈LabResult.ShowPreliminary〉（false=非表示 / true=表示） | §8、§4.1(2)、§5.2 P-02 |
| 画面全体の起動可否 | 閲覧制限（VIP）患者では、閲覧権限を持つ利用者以外は本画面を開けない（呼出元でブロック） | 患者状態（閲覧制限）＋ 利用者の閲覧権限 | §8、§10 |

## 4. 検索・一覧（グリッド）設計

### 4.1 結果一覧グリッド（dgvResults）

**(1) 検索パラメータ（入力条件）**

| No | 条件項目 | 対応する画面項目ID | 型 | 必須 | 検索方式 | 備考 |
|---|---|---|---|---|---|---|
| 1 | 患者ID | txtPatientId | 文字列 | ○ | 完全一致 | 画面で変更不可 |
| 2 | 検査日From/To | dtpDateFrom / dtpDateTo | 日付 | ○ | 範囲（両端含む） | |
| 3 | 検査種別コード | cmbTestCategory | 文字列 | - | 完全一致 | 「全て」選択時は条件に含めない |
| 4 | 基準値外のみ | chkAbnormalOnly | 真偽 | - | - | ON時、基準値外フラグ=1 の行のみ |
| 5 | 確定区分 | （画面項目なし） | 固定 | - | - | 確定のみ。設定（§8 速報表示）ON時は速報も含む |

**(2) 表示列定義**

| No | 列名（ヘッダ） | 列ID | 型 | 幅 | 表示順 | 書式 | バインド元（返却項目） |
|---|---|---|---|---|---|---|---|
| 1 | 検査日 | colTestDate | 日付 | 100 | 1 | yyyy/MM/dd | 検査実施日 |
| 2 | 種別 | colCategory | 文字列 | 90 | 2 | | 検査種別名 |
| 3 | 項目名 | colItemName | 文字列 | 160 | 3 | | 検査項目名 |
| 4 | 結果値 | colResultValue | 文字列 | 90 | 4 | 右寄せ | 結果値 |
| 5 | 単位 | colUnit | 文字列 | 60 | 5 | | 単位 |
| 6 | 基準値 | colReferenceRange | 文字列 | 110 | 6 | 下限〜上限 | 基準値下限・上限 |
| 7 | 判定 | colJudge | 文字列 | 50 | 7 | 中央寄せ | H / L / 空欄（§5.3 条件→結果表） |
| 8 | 確定 | colStatus | 文字列 | 60 | 8 | | 確定区分（確定/速報） |

行の強調：判定が H または L の行は行背景を強調色にする。速報行は文字色をグレーにする。

**(3) 返却項目（取得カラム）**

| No | 返却項目 | 取得元テーブル.カラム | 型 | 備考 |
|---|---|---|---|---|
| 1 | 検査依頼番号 | t_lab_result.request_no | 文字列 | 非表示。詳細画面への引き渡しに使用 |
| 2 | 検査実施日 | t_lab_result.test_date | 日付 | |
| 3 | 検査種別名 | m_test_category.category_name | 文字列 | 結合取得 |
| 4 | 検査項目名 | m_test_item.item_name | 文字列 | 結合取得 |
| 5 | 結果値 | t_lab_result.result_value | 文字列 | |
| 6 | 単位 | m_test_item.unit | 文字列 | |
| 7 | 基準値下限 / 上限 | m_test_item.ref_low / ref_high | 数値 | |
| 8 | 基準値外フラグ | t_lab_result.abnormal_flag | 数値 | 判定列の元 |
| 9 | 確定区分 | t_lab_result.confirm_status | 文字列 | |

**(4) 検索仕様の共通ルール**

- 0件時：グリッドを空にし M-03 を情報表示（エラーではない）
- 件数上限：2,000件。超過時は先頭2,000件を表示し M-04 を警告表示
- 初期ソート：検査日 降順 → 表示順（検査項目マスタの並び順）昇順
- 行ダブルクリック：P-03（詳細画面を開く）

## 5. 処理設計

### 5.1 処理一覧

| No | 処理名 | 契機（画面操作/ライフサイクル） | 概要 |
|---|---|---|---|
| P-01 | 初期表示 | 画面 Load | 患者情報表示・種別マスタ取得・条件初期化 |
| P-02 | 結果検索 | btnSearch Click | 入力チェック → 検索 → 一覧表示 |
| P-03 | 詳細表示 | 行ダブルクリック / btnDetail Click | 選択行の詳細画面を開く |

### 5.2 処理詳細

#### P-02: 結果検索

| 項目 | 内容 |
|---|---|
| 契機 | btnSearch の Click |
| 入力 | §4.1(1) の検索パラメータ |
| 出力 / 画面反映 | 結果一覧の再表示、件数の表示 |
| 前提条件 | 入力チェック（§3.3）を通過していること |
| 事後条件 | 「詳細」Button は無効に戻る（行選択が解除されるため） |

**処理の流れ**

1. 入力チェック（§3.3）を行う。NG の場合はメッセージを表示し中断する
2. 検索パラメータを組み立てる。検査種別が「全て」の場合、種別条件は付けない。設定〈LabResult.ShowPreliminary〉が ON のとき、確定区分の絞り込みを外し速報も対象に含める
3. サーバへ検索を依頼し、結果を受け取る（SQL は §6.2 Q-01）
4. 各行の判定列を §5.3 の条件→結果表に従い設定する
5. グリッドへバインドし、初期ソートを適用する
6. 0件・上限超過の場合は §4.1(4) のとおりメッセージを表示する

#### P-03: 詳細表示

| 項目 | 内容 |
|---|---|
| 契機 | dgvResults の行ダブルクリック、または btnDetail の Click |
| 入力 | 選択行の検査依頼番号（非表示の返却項目 No.1） |
| 出力 / 画面反映 | 検査結果詳細画面をモーダルで表示 |
| 前提条件 | 行が1行選択されていること |
| 事後条件 | 詳細画面を閉じた後、一覧の選択状態・スクロール位置は保持される |

**処理の流れ**

1. 選択行から検査依頼番号を取得する
2. 検査結果詳細画面に検査依頼番号・患者IDを渡して開く

### 5.3 複雑ロジックの記述

**判定列（H / L / 空欄）の決定 — L2: 条件→結果表**

| 条件 | 結果 |
|---|---|
| 結果値が数値であり、基準値上限を超える | 「H」を表示（行を強調色） |
| 結果値が数値であり、基準値下限を下回る | 「L」を表示（行を強調色） |
| 結果値が数値であり、基準範囲内 | 空欄 |
| 結果値が数値でない（例:「陰性」等の文字結果） | 空欄（基準値判定を行わない） |
| 基準値の下限・上限がマスタ未設定 | 空欄（判定不能。エラーにしない） |

## 6. ユースケース設計（データ入出力）

### 6.1 ユースケース一覧

| UC-ID | ユースケース名（業務動詞） | 種別 | 契機（処理No） | 概要 |
|---|---|---|---|---|
| UC-01 | 検査種別の一覧を取得する | 照会 | P-01 | 種別プルダウンの選択肢（有効な種別のみ） |
| UC-02 | 患者の基本情報を取得する | 照会 | P-01 | 患者ID から氏名を取得し画面表示 |
| UC-03 | 患者の検査結果を検索する | 照会 | P-02 | 期間・種別・基準値外・確定区分で絞り込み一覧を返す |

P-03（詳細表示）は本画面では DB 入出力を伴わない（詳細画面側の UC → DD-LAB-0022）。

### 6.2 ユースケース定義

#### UC-03: 患者の検査結果を検索する

| 項目 | 内容 |
|---|---|
| 種別 | 照会（Query） |
| 契機 | P-02（→ §5.2） |
| 事前条件 | 患者が特定されていること。検索期間が正当（From≦To かつ 2年以内）であること |
| 事後条件 | なし（参照のみ。状態を変更しない） |
| トランザクション境界 | なし（単一 SELECT） |
| 同時実行・排他 | 考慮なし（現行どおり） |

**入力（→ アプリケーション層の入力DTOに対応。§4.1(1) と1対1）**

| No | 項目 | 型 | 必須 | 制約・意味 |
|---|---|---|---|---|
| 1 | 患者ID | 文字列 | ○ | 完全一致 |
| 2 | 検査日From / To | 日付 | ○ | 範囲（両端含む）。From≦To、期間2年以内 |
| 3 | 検査種別コード | 文字列 | - | 未指定＝全種別 |
| 4 | 基準値外のみ | 真偽 | - | true のとき基準値外の結果のみ |
| 5 | 速報を含む | 真偽 | - | 設定値（§8）に由来。画面入力ではない |

**出力（→ アプリケーション層の出力DTOに対応。§4.1(3) と1対1＋メタ情報）**

| No | 項目 | 型 | 意味 |
|---|---|---|---|
| 1 | 明細一覧 | 明細の配列 | 各明細＝§4.1(3) No.1〜9（検査依頼番号・検査実施日・種別名・項目名・結果値・単位・基準値下限/上限・基準値外フラグ・確定区分） |
| 2 | 打切りフラグ | 真偽 | 上限2,000件で打切ったとき true |

**業務ルール（不変条件）**

- 明細は検査日 降順 → 検査項目の表示順 昇順で返す
- 上限2,000件で打切る（打切ってもエラーにしない。フラグで通知）
- 基準値外の判定値（H/L）の決定は §5.3 の条件→結果表による（判定の実施層は移行設計側の判断とし、判定規則のみを契約とする）

**結果とエラーの対応**

| 結果 | 分類 | 対応メッセージ | 備考 |
|---|---|---|---|
| 成功（0件含む） | 正常 | 0件時 M-03 | 0件は業務エラーではない |
| 上限超過 | 正常（打切りフラグ true） | M-04 | 表示は継続 |
| 期間指定不正 | 業務エラー（結果として返す） | M-01 / M-02 | 現行は画面側チェック（§3.3）。UC でも入力不変条件として保持 |
| サーバ通信失敗 | 技術例外 | 通信エラー表示（§9.2） | |

#### UC-01 / UC-02（単純照会のため要点のみ）

| UC | 入力 | 出力 | 備考 |
|---|---|---|---|
| UC-01 | なし | 種別（コード・名称）の一覧 | 有効フラグ=1 のみ。表示順は未確認（→ §11） |
| UC-02 | 患者ID | 患者氏名 | 該当なしは技術例外ではなく「不在」を結果で返す |

### 6.3 UC → CRUD 対応表

| UC-ID | テーブル | C/R/U/D | 主な条件 | 主な対象カラム |
|---|---|---|---|---|
| UC-01 | m_test_category | R | 有効フラグ=1 | category_code, category_name |
| UC-02 | m_patient | R | 患者ID | patient_name |
| UC-03 | t_lab_result / m_test_item / m_test_category | R | 患者ID・検査日範囲・種別・基準値外・確定区分 | UC-03 出力の明細項目 |

本画面からの C/U/D は無い（参照専用。確認済み: 対象クラスから Execute される ServerControl は SELECT のみ）。

### 6.4 SQL定義（現行挙動の参考）

```sql
-- Q-01: 検査結果一覧検索（対応 UC: UC-03）
SELECT
    r.request_no,
    r.test_date,
    c.category_name,
    i.item_name,
    r.result_value,
    i.unit,
    i.ref_low,
    i.ref_high,
    r.abnormal_flag,
    r.confirm_status
FROM t_lab_result r
    INNER JOIN m_test_item     i ON i.item_code     = r.item_code
    INNER JOIN m_test_category c ON c.category_code = i.category_code
WHERE r.patient_id = @patientId
  AND r.test_date BETWEEN @dateFrom AND @dateTo
  /* 種別「全て」以外のとき */ AND i.category_code = @categoryCode
  /* 基準値外のみ ON のとき */ AND r.abnormal_flag = 1
  /* 速報表示 OFF のとき */    AND r.confirm_status = '1'  -- 1:確定
ORDER BY r.test_date DESC, i.display_order ASC
```

| 項目 | 内容 |
|---|---|
| 対応 UC | UC-03 |
| 実行経路 | 検索 Button → サーバコマンド（検査結果一覧取得） |
| 想定件数 / 性能上の注意 | 患者・2年範囲で最大数千件。上限2,000件で打切り |

※ 本 SQL は現行実装の記録であり、移行先が結合構造・打切り方式を踏襲する契約ではない。契約は UC-03 の定義。

### 6.5 テーブル・カラム定義（本機能が参照する範囲のみ）

| テーブル | カラム | 型 | 本機能での用途 | 実在確認 |
|---|---|---|---|---|
| t_lab_result | request_no, test_date, item_code, result_value, abnormal_flag, confirm_status, patient_id | - | 検索・一覧表示 | ○（エンティティ定義で確認） |
| m_test_item | item_code, item_name, unit, ref_low, ref_high, category_code, display_order | - | 結合・基準値判定・並び順 | ○（エンティティ定義で確認） |
| m_test_category | category_code, category_name | - | 種別プルダウン・結合 | ○（エンティティ定義で確認） |

## 7. 外部連携

該当なし（確認済み: 対象クラスおよび呼び出す ServerControl に外部送信・ファイル出力・印刷の処理なし）。

## 8. 設定値

| 設定キー / 判定 | 区分 | 既定値 | 本機能での影響 | 影響箇所（章参照） |
|---|---|---|---|---|
| LabResult.ShowPreliminary | 設定 | false | true のとき速報結果も一覧に含め、速報行をグレー表示する。false のとき確定区分列を非表示 | §3.5、§4.1(1) No.5、§4.1(2)、§5.2 P-02 手順2、§6.2 UC-03 入力 No.5 |
| 閲覧制限（VIP）＋利用者の閲覧権限 | 権限 | - | 閲覧制限患者は、閲覧権限を持つ利用者以外は本画面を開けない | §3.5、§10 |

## 9. メッセージ・エラー処理

### 9.1 メッセージ一覧

| ID | 種別 | 文言 | 表示契機 | 応答による分岐 |
|---|---|---|---|---|
| M-01 | 警告 | 検査日の期間指定が正しくありません。 | §3.3 From＞To | OK のみ。検索中止 |
| M-02 | 警告 | 検索期間は2年以内で指定してください。 | §3.3 期間超過 | OK のみ。検索中止 |
| M-03 | 情報 | 該当する検査結果がありません。 | P-02 で0件 | OK のみ |
| M-04 | 警告 | 該当件数が上限を超えたため、先頭2,000件のみ表示します。 | P-02 で上限超過 | OK のみ。表示は継続 |

### 9.2 異常時の動作

| 事象 | 検知 | 画面の動作 | データへの影響 |
|---|---|---|---|
| サーバ通信失敗 | 検索依頼の例外 | 通信エラーメッセージを表示し、一覧は直前の表示内容を保持 | なし（参照のみ） |

## 10. 医療安全上の留意点

- **患者識別**: 本画面は呼出元から渡された患者IDに固定され、画面内で患者を切り替えられない。表示中も患者ID・氏名を常時表示し、別患者の結果と取り違えない構造とする。
- **速報結果の扱い**: 速報（未確定）結果は確定値と異なる可能性があるため、既定では表示しない。設定で表示する場合も「速報」の明示とグレー表示で確定結果と視覚的に区別する（§4.1(2)）。
- **基準値判定の限界**: 判定列は基準値マスタに依存する。マスタ未設定の項目は判定しない（空欄）仕様であり、「空欄＝正常」ではないことを運用上周知する必要がある。
- **表示の打切り**: 件数上限で打切った場合は必ず警告（M-04）を出し、全件表示と誤認させない。
- 〔推定〕速報を既定で非表示にしているのは、確定前の暫定値を確定値と取り違えさせないための設計と考えられる（既定値 false は §8 で確認済みだが、その意図を明示するコメントはコード上に無いため推定）。

## 11. 未確認事項・申し送り

| No | 内容 | 未確認の理由（どこまで追ったか） |
|---|---|---|
| 1 | 検査項目マスタの display_order が未設定（NULL）の場合の並び順 | SQL の ORDER BY までは確認。NULL 時の実挙動（DB照合順）は未検証 |
| 2 | UC-01 種別一覧の表示順 | ServerControl の SELECT に ORDER BY なし。現行はDB返却順に依存 |

## 付録A. 解析対象ファイル一覧（網羅の証明）

| ファイル | 用途 |
|---|---|
| FrmLabResultList.cs | イベント本体 |
| FrmLabResultList.Designer.cs | 画面項目・イベント結線・グリッド列定義 |
| ServerControls/Lab/LabResultListSelect.cs | 検索 SQL（Q-01） |
| db/tables/TLabResult/TLabResult（エンティティ定義） | テーブル実在確認 |
| db/tables/MTestItem/MTestItem（エンティティ定義） | 同上 |
| db/tables/MTestCategory/MTestCategory（エンティティ定義） | 同上 |
| 設定定義層 LabSettings（設定定義） | 設定キー確認 |

## 付録B. トレーサビリティ（必要な場合のみ・最小限）

| 設計書の記載箇所 | 実装位置（file:line） |
|---|---|
| §5.3 判定列の決定 | FrmLabResultList.cs:412 |
