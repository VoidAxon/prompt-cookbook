# 機能理解前置(2.0)追加 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `tools/generate-testdoc/SKILL.md` の Step 2 冒頭に「機能の全体把握(差分を見る前に)」小節 `2.0` を追加し、変更テスト前に機能を理解させる。

**Architecture:** 既存の Step 2 導入段落の直後・`### 2.1` の直前に `### 2.0` 小節を 1 つ挿入するだけ。既存行は一切改変しない。番号は `2.0` を使い、2.1〜2.5 とその外部参照(`references/test-writing.md` の「Step 2.3」「Step 2.5」)を不変に保つ。

**Tech Stack:** Markdown(skill ドキュメント)。コード・テストフレームワークなし。検証は差分確認・参照健全性・行為実跑で行う。

参照する設計: `tools/generate-testdoc/docs/design-2026-06-10-feature-understanding-step.md`

---

### Task 1: SKILL.md に `2.0` 小節を挿入する

**Files:**
- Modify: `tools/generate-testdoc/SKILL.md`(Step 2 導入段落の直後、`### 2.1 変更箇所の列挙` の直前)

- [ ] **Step 1: 挿入位置を確認する**

Read で `tools/generate-testdoc/SKILL.md` の Step 2 周辺を開き、次のアンカーを確認する：

```markdown
## Step 2: 影響範囲を確認(最重要)

差分の行だけを見るテストは本質的なリスクを見落とす。**変更行を起点に「動作が変わりうる全パス」**を以下の順で確認する。

### 2.1 変更箇所の列挙
```

導入段落(「差分の行だけを見る…確認する。」)と `### 2.1 変更箇所の列挙` の間が挿入位置。

- [ ] **Step 2: `2.0` 小節を挿入する**

Edit で、`old_string` を次の 2 行(導入段落末尾＋空行＋ `### 2.1` 見出し)にして一意に特定し、その間に `2.0` 小節を差し込む。

old_string:
```
差分の行だけを見るテストは本質的なリスクを見落とす。**変更行を起点に「動作が変わりうる全パス」**を以下の順で確認する。

### 2.1 変更箇所の列挙
```

new_string:
```
差分の行だけを見るテストは本質的なリスクを見落とす。**変更行を起点に「動作が変わりうる全パス」**を以下の順で確認する。

### 2.0 機能の全体把握(差分を見る前に)

変更行を見る前に、**この変更が属する機能そのものを先に理解する**。何のための機能か分からないまま差分だけを追うと、技術的には正しくても業務的に意味のない観点になる。最低限、次を一文で言えるようにする：

- **誰が・何のために**使う機能か(対象ユーザー・業務上の目的)
- **どんな業務フロー**の中で、どの画面・操作から到達するか

**深さは変更規模に合わせる(自適応)：**

- 小規模修正(表示文言・軽微なバグ修正など)：機能の目的を 1〜2 文で押さえれば十分。深追いしない
- 業務ロジック・フロー・状態遷移に関わる変更：到達経路と前後の業務フローまで広げて把握する

**情報源は軽い順に。発散させない：**

1. まず PR 説明文・diff・2.3 で業務名称を確認する際に読むコードの範囲で理解する
2. それでも**変更の業務的意図が掴めない場合のみ**、変更箇所の周辺コード(同一画面・クラスの他部分、主要な呼出元)を**定向に**読む。広く読み漁らない
3. 周辺コードを読んだ時は、**なぜ読んだかを一言メモ**する(例：`// 機能把握: 到達経路がdiffから不明なため呼出元を確認`)

ここで得た理解は **3.1 のグループ軸(業務機能エリア)と Step 4 の観点・期待値に反映する**。この把握自体は単独のテストケース・出力にはしない(内部理解にとどめる)。

### 2.1 変更箇所の列挙
```

- [ ] **Step 3: 差分の最小性を確認する**

Run: `git diff tools/generate-testdoc/SKILL.md`
Expected: 追加は `2.0` 小節(上記 new_string の差し込み分)のみ。**既存行の削除・改変が 0 であること**(diff に `-` 行が無い、または既存テキストの変更が無い)。

- [ ] **Step 4: 番号衝突と参照健全性を確認する**

Run: `git grep -n "2\.0 機能の全体把握" tools/generate-testdoc/SKILL.md` で 2.0 が 1 箇所追加されたことを確認。
Run: `git grep -n "Step 2\.3\|Step 2\.5\|2\.2 で把握" tools/generate-testdoc/` で、既存の 2.3 / 2.5 / 2.2 参照が**従来通り残っている**(番号を動かしていない)ことを確認。
Expected: 2.1〜2.5 の見出し番号は変化なし、`references/test-writing.md` の「Step 2.3」「Step 2.5」参照も有効のまま。

- [ ] **Step 5: コミット**

```bash
git add tools/generate-testdoc/SKILL.md
git commit -m "feat: generate-testdoc に機能理解前置(2.0)を追加する" -m "差分を見る前に、変更が属する機能の業務目的・業務フローを把握する前置小節 2.0 を Step 2 冒頭へ追加。深さ・情報源は変更規模に自適応し、理解は 3.1 分組と Step 4 観点へ反映(出力はしない)。既存 2.1〜2.5 の番号・参照は不変。" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 行為実跑で前置が機能することを確認する

**Files:**
- 変更なし(検証のみ)

- [ ] **Step 1: 実在の merged PR 番号を取得する**

Run: `gh pr list --repo medley-inc/mall4 --state merged --limit 20 --json number,title`
業務ロジック・フロー寄りの PR を 1 件選ぶ(記憶の番号を使わず、出力から選ぶ)。

- [ ] **Step 2: クリーンな subagent に処理させる**

`general-purpose` subagent を dispatch し、**「`tools/generate-testdoc/SKILL.md` に従って merged PR #<番号> を処理せよ」とだけ指示する(`2.0` の存在は伝えない)**。完了後、実際に辿った手順の正直な時系列報告を求める。

- [ ] **Step 3: 期待する挙動を確認する**

Expected(報告から確認):
- 差分の変更行を列挙する前に、**機能の業務目的・到達経路を自発的に把握**している
- 変更規模に応じて深さを調整している(小規模なら 1〜2 文、業務ロジックなら業務フロー層まで)
- 機能把握のための周辺コード読みは PR 説明文/diff で足りない時のみで、読んだ場合は理由メモがある
- 機能概要を**独立した出力・ケースにはしていない**(内部理解にとどめている)

不合格(把握ステップが飛ばされた等)の場合、`2.0` の文言を強める(例: 「差分を見る前に必ず」)改訂を検討して Task 1 に戻る。

---

## Self-Review(記録)

- **Spec coverage**: 設計の「配置と番号」「2.0 本文」「2.0 と 2.4 の分担」「スコープ(やらないこと)」「検証(差分最小性・参照健全性・行為実跑)」は Task 1(挿入・差分確認・参照確認・コミット)と Task 2(行為実跑)で全てカバー。スキーマ/テンプレート不変はファイル変更を SKILL.md のみに限定することで担保。
- **Placeholder scan**: TBD/TODO なし。挿入する本文は確定版を全文掲載。
- **Type consistency**: コードなし。見出し番号 `2.0` は全タスクで一貫。
