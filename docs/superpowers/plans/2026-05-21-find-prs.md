# find-prs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GitHub 組織配下の全仓储から PR タイトル検索を行い、oneline/tree/table 形式で結果を整形・出力する Claude Code 用カスタム skill `find-prs` を実装する。

**Architecture:** 純 Markdown skill 方式。`tools/find-prs/SKILL.md` に AI 向け実行手順を記述し、AI が `gh search prs` と `gh pr view` を直接呼び出して JSON を加工・整形する。外部スクリプト不要。`examples.md` でサンプル入出力を提供し、`README.md` の skill 一覧表へ 1 行追加する。

**Tech Stack:** Markdown（SKILL.md / examples.md / README.md）、GitHub CLI（`gh search prs` / `gh pr view`）、PowerShell（`Set-Clipboard`、Windows のみ）。

**Reference Spec:** [docs/superpowers/specs/2026-05-21-find-prs-design.md](../specs/2026-05-21-find-prs-design.md)

---

## File Structure

| パス | 操作 | 責務 |
|------|------|------|
| `tools/find-prs/SKILL.md` | 新規作成 | AI 向け実行指示書。frontmatter・コマンド規約・実行手順・出力形式規約・エラー処理 |
| `tools/find-prs/examples.md` | 新規作成 | 入出力サンプル（5 ケース） |
| `README.md` | 修正（tools 表へ 1 行追加 + ディレクトリ図更新） | プロジェクトインデックスへの掲載 |

---

## Task 1: SKILL.md を作成する

**Files:**
- Create: `tools/find-prs/SKILL.md`

- [ ] **Step 1: SKILL.md を一括書き出し**

ファイル全文（以下を `tools/find-prs/SKILL.md` に書き込む）:

````markdown
---
name: find-prs
description: Use when the user invokes /find-prs to search PRs by title across all repositories in a GitHub organization. Outputs results in oneline / tree / table formats and optionally copies the oneline form to the clipboard.
---

# find-prs

## Overview

GitHub 組織配下の全リポジトリから、PR タイトルにキーワードを含む PR を一括検索し、oneline・tree・table の 3 形式で結果を整形して出力するスラッシュコマンド。oneline は Windows 環境で自動的にクリップボードへコピーされ、Excel / Kintone 等への貼り付けを容易にする。

あなたは、複数の長期ブランチをまたぐ同一要件の修正状況を集計するアシスタントです。検索 → 集計 → 整形を機械的に正確に実行してください。

## Usage

```
/find-prs <keyword> [--org <name>] [--state <s>] [--format <f>] [--copy|--no-copy]
```

### 引数・オプション

| 名前 | 必須 | デフォルト | 値域 | 説明 |
|------|------|-----------|------|------|
| `<keyword>` | はい | — | 任意文字列 | PR タイトル検索キーワード（例: `1234`、`患者一覧`） |
| `--org` | いいえ | `medley-inc` | 任意組織名 | GitHub 組織名 |
| `--state` | いいえ | `all` | `open` / `closed` / `merged` / `all` | PR 状態フィルタ |
| `--format` | いいえ | `oneline` | `oneline` / `tree` / `table`、カンマ区切り複数指定可 | 出力形式 |
| `--copy` / `--no-copy` | いいえ | `--copy` | フラグ | oneline をクリップボードへコピーするか（Windows のみ有効） |

呼び出し例：

```
/find-prs 1234
/find-prs 1234 --state merged --format tree,oneline
/find-prs 患者一覧 --format table
/find-prs 1234 --org medley-inc --format tree,table,oneline --no-copy
```

---

## Step 1: 前提チェック

`gh auth status` を実行し、認証されていなければ「`gh auth login` を実行してください」と告知し中断する。

プラットフォームを判定する（Windows / macOS / Linux）。クリップボード処理の分岐に使う。

---

## Step 2: 引数パース

ユーザー入力から `<keyword>` と各オプションを抽出する。

- `<keyword>` は最初の位置引数。空ならエラー「キーワードを指定してください」で中断
- `--org` 未指定なら `medley-inc`
- `--state` 未指定なら `all`
- `--format` 未指定なら `oneline`。カンマ区切りなら配列化（例: `tree,oneline` → `["tree","oneline"]`）。不正な値があれば中断
- `--copy` / `--no-copy` 未指定なら `--copy`

---

## Step 3: PR を検索

`gh search prs` で組織横断検索を実行する。

```
gh search prs --owner <org> "<keyword>" --match title \
  [状態フラグ] \
  --json number,title,repository,state,url --limit 100
```

**状態フラグのマッピング：**

| `--state` | `gh search prs` への渡し方 |
|-----------|---------------------------|
| `open` | `--state open` |
| `closed` | `--state closed` |
| `merged` | `--merged`（`--state merged` は不可） |
| `all` | フラグ未指定（既定動作） |

**結果ハンドリング：**

- 結果 0 件 → 「指定キーワードに一致する PR が見つかりませんでした」と出力して正常終了
- 結果が 100 件ちょうど → 「結果が上限 100 件で切り捨てられている可能性があります。より具体的なキーワードをご検討ください」と末尾に警告

---

## Step 4: base 分支を補完

`gh search prs` の JSON 出力には `baseRefName` が含まれない。各 PR について追加取得する：

```
gh pr view <number> --repo <owner/repo> --json baseRefName
```

複数の `gh pr view` 呼び出しは **1 メッセージ内で並列実行**してレイテンシを下げる。

個別の `gh pr view` 失敗時は、当該 PR をスキップして警告ログを残し、他の PR の処理を継続する。

---

## Step 5: 集計・並び替え

検索結果を `repository.nameWithOwner` で groupBy する。

**oneline 用の並び順：**

1. `baseRefName == "develop"` の条目を先頭にまとめる
2. 1 のグループ内では repo 名（`nameWithOwner` ではなく `name` 部分）の辞書順
3. その後に他の分支条目を分支名の辞書順で並べる

**tree / table 用の並び順：**

1. repo 名の辞書順
2. 各 repo 内では分支名の辞書順

---

## Step 6: 整形出力

`--format` で指定された形式を **指定順** に出力する。形式間は空行 1 行で区切る。

### 6.1 oneline 形式

ルール：
- `baseRefName == "develop"` → `<repo>最新(#PR番号)`
- それ以外 → `<分支名>(#PR番号)`
- 区切り: `, `（半角カンマ + 半角スペース）

例：

```
mall4最新(#12), mall3最新(#34), release-2024(#11), release-2025(#13)
```

### 6.2 tree 形式

ルール：
- 行頭に repo 名のみ（組織名なし）
- 次行から半角スペース 2 個でインデント、ツリー記号なし
- 各エントリは `<分支名>: <URL>`
- 複数 repo 間に空行を入れない（密に詰める）

例：

```
mall4
  develop: https://github.com/medley-inc/mall4/pull/12
  release-2025: https://github.com/medley-inc/mall4/pull/13
mall3
  develop: https://github.com/medley-inc/mall3/pull/34
```

### 6.3 table 形式

Markdown 表形式：

| 仓储:分支 | PR# | State | Title | URL |
|----------|-----|-------|-------|-----|
| `<repo>:<分支名>`（develop の簡略化なし） | `#<番号>` | `open` / `closed` / `merged` | PR タイトル原文 | PR の HTML URL |

例：

| 仓储:分支 | PR# | State | Title | URL |
|----------|-----|-------|-------|-----|
| mall4:develop | #12 | merged | 1234 患者一覧の検索条件修正 | https://github.com/medley-inc/mall4/pull/12 |
| mall4:release-2025 | #13 | merged | 1234 患者一覧の検索条件修正 | https://github.com/medley-inc/mall4/pull/13 |

---

## Step 7: クリップボード反映

以下の全条件を満たす場合のみ実行する：

- `--copy`（または未指定で既定の `--copy`）
- プラットフォームが Windows
- `--format` に `oneline` が含まれる

実行コマンド：

```powershell
Set-Clipboard -Value "<oneline 出力>"
```

**スキップ条件と告知：**

| 状況 | 動作 |
|------|------|
| `--no-copy` | スキップ（告知不要） |
| Windows 以外 | 「macOS/Linux ではクリップボード未対応のためスキップしました」と告知 |
| `--format` に oneline 未含有 | 「コピー対象（oneline）が出力に含まれないためスキップしました」と告知 |

---

## Step 8: 完了報告

末尾に以下を 1〜2 行で出力する：

- ヒット件数（例: 「3 件ヒット」）
- クリップボードコピー有無（例: 「oneline をクリップボードへコピーしました」 / 「クリップボードコピーはスキップしました」）
- 100 件警告（該当時）

---

## 参考：gh コマンド一覧

```bash
# 認証チェック
gh auth status

# PR 検索（全状態）
gh search prs --owner medley-inc "1234" --match title \
  --json number,title,repository,state,url --limit 100

# PR 検索（merged のみ）
gh search prs --owner medley-inc "1234" --match title --merged \
  --json number,title,repository,state,url --limit 100

# base 分支の取得
gh pr view 12 --repo medley-inc/mall4 --json baseRefName
```
````

- [ ] **Step 2: frontmatter と主要フィールドの整合を確認**

Run:
```
grep -E "^name:|^description:" tools/find-prs/SKILL.md
```
Expected:
```
name: find-prs
description: Use when the user invokes /find-prs ...
```

- [ ] **Step 3: 必須セクションの存在を確認**

Run:
```
grep -E "^## (Usage|Step [1-8])" tools/find-prs/SKILL.md | wc -l
```
Expected: `9` 行（Usage + Step 1〜8）

- [ ] **Step 4: --format 全値の言及を確認**

Run:
```
grep -E "oneline|tree|table" tools/find-prs/SKILL.md | wc -l
```
Expected: 10 以上（各形式を複数箇所で言及）

- [ ] **Step 5: コミット**

```bash
git add tools/find-prs/SKILL.md
git commit -m "$(cat <<'EOF'
feat: find-prs skill の SKILL.md を追加する

GitHub 組織配下を横断して PR タイトル検索を行い、oneline/tree/table
の 3 形式で結果を整形・出力する skill 本体を実装する。oneline は
Windows でクリップボードへ自動コピーされ、貼り付け作業を省力化する。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: examples.md を作成する

**Files:**
- Create: `tools/find-prs/examples.md`

- [ ] **Step 1: examples.md を一括書き出し**

ファイル全文（以下を `tools/find-prs/examples.md` に書き込む）:

````markdown
# find-prs 使用例

## 例 1: 既定設定（oneline + 自動コピー）

入力：
```
/find-prs 1234
```

出力：
```
mall4最新(#12), mall3最新(#34), release-2024(#11), release-2025(#13)

4 件ヒット
oneline をクリップボードへコピーしました
```

## 例 2: tree + oneline 併用（merged のみ）

入力：
```
/find-prs 1234 --format tree,oneline --state merged
```

出力：
```
mall4
  develop: https://github.com/medley-inc/mall4/pull/12
  release-2025: https://github.com/medley-inc/mall4/pull/13
mall3
  develop: https://github.com/medley-inc/mall3/pull/34

mall4最新(#12), mall3最新(#34), release-2025(#13)

3 件ヒット
oneline をクリップボードへコピーしました
```

## 例 3: table 単独

入力：
```
/find-prs 患者一覧 --format table
```

出力：
```
| 仓储:分支 | PR# | State | Title | URL |
|----------|-----|-------|-------|-----|
| mall4:develop | #12 | merged | 1234 患者一覧の検索条件修正 | https://github.com/medley-inc/mall4/pull/12 |
| mall4:release-2025 | #13 | merged | 1234 患者一覧の検索条件修正 | https://github.com/medley-inc/mall4/pull/13 |

2 件ヒット
コピー対象（oneline）が出力に含まれないためスキップしました
```

## 例 4: 0 件ヒット

入力：
```
/find-prs 存在しないキーワード
```

出力：
```
指定キーワードに一致する PR が見つかりませんでした
```

## 例 5: 100 件上限警告

入力：
```
/find-prs fix
```

出力（抜粋）：
```
fix(#1), fix(#2), ..., fix(#100)

100 件ヒット
結果が上限 100 件で切り捨てられている可能性があります。より具体的なキーワードをご検討ください
oneline をクリップボードへコピーしました
```
````

- [ ] **Step 2: 全 5 ケースの存在を確認**

Run:
```
grep -E "^## 例 [1-5]" tools/find-prs/examples.md | wc -l
```
Expected: `5`

- [ ] **Step 3: コミット**

```bash
git add tools/find-prs/examples.md
git commit -m "$(cat <<'EOF'
docs: find-prs skill の examples.md を追加する

oneline・tree・table の 3 形式と各境界ケース（0 件・100 件上限）を
含む 5 種類の入出力サンプルを提供し、利用者が期待動作を素早く
把握できるようにする。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: README.md を更新する

**Files:**
- Modify: `README.md`（tools セクション表・ディレクトリ構成図）

- [ ] **Step 1: tools 表へ 1 行追加**

`README.md` の以下の行を見つける：

```
| [generate-testdoc](tools/generate-testdoc/SKILL.md) | `/generate-testdoc pr <no>` | PRまたはコミットの差分からテスト仕様書（Excel）を生成 |
```

その直後に以下の行を追加する：

```
| [find-prs](tools/find-prs/SKILL.md) | `/find-prs <keyword> [opts]` | 組織横断で PR タイトル検索し、oneline/tree/table で出力 |
```

- [ ] **Step 2: ディレクトリ構成図に find-prs を追加**

`README.md` のディレクトリ構成図を見つける（`└── generate-testdoc/` を含むブロック）。現状：

```
└── tools/
    ├── snap/
    │   ├── SKILL.md
    │   └── snap.ps1              # スタンドアロン利用時の PowerShell スクリプト
    ├── snappath/
    │   └── SKILL.md
    └── generate-testdoc/
        ├── SKILL.md
        ├── scripts/
        │   └── generate_testdoc.py   # Excel 生成スクリプト（要 Python + openpyxl）
        └── templates/
            └── test_spec_template.xlsx
```

これを以下に置き換える：

```
└── tools/
    ├── snap/
    │   ├── SKILL.md
    │   └── snap.ps1              # スタンドアロン利用時の PowerShell スクリプト
    ├── snappath/
    │   └── SKILL.md
    ├── find-prs/
    │   ├── SKILL.md
    │   └── examples.md
    └── generate-testdoc/
        ├── SKILL.md
        ├── scripts/
        │   └── generate_testdoc.py   # Excel 生成スクリプト（要 Python + openpyxl）
        └── templates/
            └── test_spec_template.xlsx
```

- [ ] **Step 3: tools 表に find-prs が含まれるか確認**

Run:
```
grep -E "find-prs" README.md | wc -l
```
Expected: `2`（表 + ディレクトリ図）

- [ ] **Step 4: コミット**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs: README に find-prs skill を掲載する

新規 skill `find-prs` を tools セクションの表とディレクトリ構成図に
追記し、プロジェクトの skill インデックスから発見できるようにする。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 端到端验证（手動）

**Files:** なし（実機検証）

このタスクはユーザー手動による動作確認。実装者は以下のコマンドを順番に実行し、出力が期待通りであるかをユーザーに確認してもらう。検証用キーワードは実際に複数 PR がヒットする既知の KintoneID をユーザーに指定してもらう（以下では仮に `1234` とする）。

- [ ] **Step 1: gh CLI 認証を確認**

Run:
```
gh auth status
```
Expected: ログイン済みである旨

未ログインなら `gh auth login` を実行してから次へ。

- [ ] **Step 2: oneline 既定動作を確認**

実機で以下を実行し、ユーザーに結果を確認してもらう（`1234` 部分は実在の KintoneID に差し替え）：

```
/find-prs 1234
```

確認ポイント：
- ヒット件数が妥当か
- develop 分支は `<repo>最新(#PR)` 形式で出力されているか
- 他の分支は `<分支名>(#PR)` 形式で出力されているか
- 並び順（develop 先 → 他分支） が守られているか
- クリップボードにコピーされているか（手動で他アプリへ貼り付けて確認）

- [ ] **Step 3: tree 形式を確認**

```
/find-prs 1234 --format tree
```

確認ポイント：
- 各 repo 行が組織名なしで表示されているか
- 各エントリが `<分支名>: <URL>` 形式か
- 複数 repo 間に空行が入っていないか

- [ ] **Step 4: table 形式を確認**

```
/find-prs 1234 --format table
```

確認ポイント：
- Markdown 表として正しくレンダリングされるか
- `仓储:分支` 列が `<repo>:<分支名>` 形式か
- クリップボードコピーがスキップ告知されるか

- [ ] **Step 5: 状態フィルタを確認**

```
/find-prs 1234 --state merged
```

確認ポイント：
- merged の PR のみがヒットするか
- closed や open が含まれないか

- [ ] **Step 6: 0 件ケースを確認**

```
/find-prs zzz_unlikely_keyword_xyz
```
Expected: 「指定キーワードに一致する PR が見つかりませんでした」

- [ ] **Step 7: 不正引数を確認**

```
/find-prs 1234 --format invalid
```
Expected: 不正な `--format` 値である旨のエラー出力 + 中断

---

## 全タスク完了後

すべての commit が完了したら：

```bash
git log --oneline -4
```

Expected（順序は逆順）：
```
<sha> docs: README に find-prs skill を掲載する
<sha> docs: find-prs skill の examples.md を追加する
<sha> feat: find-prs skill の SKILL.md を追加する
<sha> docs: find-prs skill の設計文書を追加する
```
